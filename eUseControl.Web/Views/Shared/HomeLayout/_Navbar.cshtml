<!-- Navbar Start -->
<div class="container-fluid p-0">
    <nav class="navbar navbar-expand-lg bg-white navbar-light py-3 py-lg-0 px-lg-5">
        <a href="@Url.Action("Index","Home")" class="navbar-brand ml-lg-3">
            <h1 class="m-0 text-uppercase text-primary"><i class="fa fa-book-reader mr-3"></i>Fluently</h1>
        </a>
        <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#navbarCollapse">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-between px-lg-3" id="navbarCollapse">
            <div class="navbar-nav mx-auto py-0">
                <a href="@Url.Action("Index","Home")" class="nav-item nav-link active">Home</a>
                <a href="@Url.Action("Courses","Home")" class="nav-item nav-link">Courses</a>
            </div>
            @if (Model != null && Model.NavbarView != null && Model.NavbarView.Authenticated == false)
            {
                <a href="@Url.Action("Register","Registration")" class="btn btn-primary py-2 px-4 d-none d-lg-block">Join Us</a>
                <a href="@Url.Action("Login","Login")" style="margin-left: 10px;" class="btn btn-primary py-2 px-4 d-none d-lg-block">Login</a>
            }
            else
            {
                <a href="@Url.Action("Logout","Login")"  class="btn btn-primary py-2 px-4 d-none d-lg-block">Logout</a>

            }

        </div>
    </nav>
</div>
<!-- Navbar End -->
